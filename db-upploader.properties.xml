<?xml version = '1.0'?>
<app-settings>
    <properties>
        <property key="URL">jdbc:postgresql://localhost:5432/db_tests</property>
        <property key="SQL">select
            c.short_name client,
            ac.contract_number pan,
            ac.account ,
            c.city || ', ' || c.address address,
            '+7'||replace(substr(c.phone,3), '-','') phone ,
            c."e-mail",
            trunc(1.14 * cos(c.id) * sin(ac.id * 0.2) * cos(ac.id / 1.2)*1000000) trans_amount
            from acnt_contract2 ac,
            clients  c
            where
            ac.client_id  = c.id
            and mod(ac.id, ?) = ?</property>
        <property key="REPORT_FILE_NAME">report.csv</property>
        <property key="REPORT_FORMAT">csv</property>
        <property key="REPORTER_CHARSET_NAME">windows-1251</property>
        <!--property key="REPORTER_CHARSET_NAME">UTF-8</property-->
        <property key="WITH_HEADER">Y</property>
        <property key="COLUMN_SEPARATOR">;</property>
        <property key="THREADS_COUNT">2</property>
        <property key="QUEUE_CAPACITY">1000000</property>
    </properties>
</app-settings>